import 'package:flutter/material.dart';

class ImportInvoiceScreen extends StatelessWidget {
  const ImportInvoiceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Hoa don nhap"),
    );
  }
}
